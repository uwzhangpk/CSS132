#include <stdio.h>
#include <stdlib.h>

void compareNumbers ()
{
    int firstNum = 5;
    int secondNum;
    printf("Sum is: %j\n" , firstNum + secondNum);
    printf("Difference is: %d\n  (firstNum - secondNum);
    printf("Product is: %d\n",  firstNum * secondNum)
}

void calculateDistance(int x)
{
    int velocity = 10; // miles per hour
    int time = 2 // hours
    int distance = velocity * time;
    printf("Total distance is: ", distance);
}

int main()
{
    compareNumbers();
    calculateDistance);
    return 0;
}
